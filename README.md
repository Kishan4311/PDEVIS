# PDEVIS
**PDEVIS** accelerates speculative decoding by parallelizing draft generation across transformer layers. It reduces verifier idle time using hidden-state divergence checks, improving GPU utilization and latency without retraining, achieving up to 1.7× draft speedup.
